# Рантайм-зависимости DotaCounters.
# tkinter входит в стандартную библиотеку Python (в Linux: python3-tk).

curl_cffi>=0.7
beautifulsoup4>=4.12.0
Pillow>=10.0.0

# Для сборки .exe дополнительно нужен pyinstaller — именно через python -m,
# чтобы сборка шла на том же Python, что и программа:
#   python -m pip install pyinstaller && python -m PyInstaller main.spec
