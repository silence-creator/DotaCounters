# Рантайм-зависимости DotaCounters.
# tkinter входит в стандартную библиотеку Python (в Linux: python3-tk).

curl_cffi>=0.7
beautifulsoup4>=4.12.0
Pillow>=10.0.0

# Для сборки .exe дополнительно нужен pyinstaller:
#   pip install pyinstaller && pyinstaller main.spec
